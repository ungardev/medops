---
const features = [
  { id: 'cie11', label: 'CIE-11 WHO', img: '/screenshots/cie11.svg' },
  { id: 'cirugia', label: 'Cirugía', img: '/screenshots/cirugia.svg' },
  { id: 'hospi', label: 'Hospitalización', img: '/screenshots/hospi.svg' },
  { id: 'sala', label: 'Sala Live', img: '/screenshots/sala.svg' },
  { id: 'pagos', label: 'Pagos P2C', img: '/screenshots/pagos.svg' },
  { id: 'sede', label: 'Multi-sede', img: '/screenshots/sede.svg' },
];
---

<section class="relative min-h-screen flex flex-col justify-start overflow-hidden pt-28">
  <!-- Background Effects - Dual glow -->
  <div class="absolute inset-0 bg-gradient-to-b from-white/[0.02] via-transparent to-transparent"></div>
  <div class="absolute top-1/4 left-1/2 -translate-x-1/2 w-[600px] h-[400px] bg-emerald-500/5 rounded-full blur-[120px]"></div>
  <div class="absolute bottom-1/4 right-1/4 w-[400px] h-[300px] bg-cyan-500/3 rounded-full blur-[100px]"></div>
  
  <div class="relative z-10 max-w-[1000px] mx-auto px-6 text-center">
    <!-- MEDOPZ Logo - más grande -->
    <img src="/medopz_fuente_blanco.svg" alt="MEDOPZ" class="w-full max-w-[280px] mx-auto mb-4" />
    
    <!-- Subtitle - tipografía refinada -->
    <h1 class="text-base sm:text-lg font-light text-white/80 tracking-wide mb-2">
      Workstation elite para médicos especialistas, pacientes e instituciones de Venezuela
    </h1>
    
    <!-- Carousel Container con glow -->
    <div class="relative mt-6 mb-4">
      <!-- Left Arrow -->
      <button id="prevBtn" class="absolute left-0 top-1/2 -translate-y-1/2 z-20 w-10 h-10 flex items-center justify-center bg-white/5 hover:bg-white/15 border border-white/10 rounded-full text-white/60 hover:text-white transition-all">
        <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" />
        </svg>
      </button>
      
      <!-- Screenshot Area - con inner glow -->
      <div id="screenshotArea" class="w-full aspect-video bg-[#030303] border border-white/8 rounded-xl overflow-hidden shadow-[inset_0_0_80px_rgba(16,185,129,0.03)]">
        <div class="w-full h-full flex items-center justify-center text-white/20 text-sm">
          Screenshot: CIE-11 WHO
        </div>
      </div>
      
      <!-- Right Arrow -->
      <button id="nextBtn" class="absolute right-0 top-1/2 -translate-y-1/2 z-20 w-10 h-10 flex items-center justify-center bg-white/5 hover:bg-white/15 border border-white/10 rounded-full text-white/60 hover:text-white transition-all">
        <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
        </svg>
      </button>
    </div>
    
    <!-- Feature Pills - estilo bento -->
    <div class="flex flex-wrap items-center justify-center gap-2">
      {features.map((feature, index) => (
        <button 
          data-index={index}
          class="feature-tab px-4 py-2 bg-white/5 border border-white/8 rounded-lg text-[11px] font-normal text-white/50 whitespace-nowrap hover:bg-white/10 hover:text-white/80 transition-all"
        >
          {feature.label}
        </button>
      ))}
    </div>
  </div>
</section>

<script>
  const features = [
    { id: 'cie11', label: 'CIE-11 WHO', img: '/screenshots/cie11.svg' },
    { id: 'cirugia', label: 'Cirugía', img: '/screenshots/cirugia.svg' },
    { id: 'hospi', label: 'Hospitalización', img: '/screenshots/hospi.svg' },
    { id: 'sala', label: 'Sala Live', img: '/screenshots/sala.svg' },
    { id: 'pagos', label: 'Pagos P2C', img: '/screenshots/pagos.svg' },
    { id: 'sede', label: 'Multi-sede', img: '/screenshots/sede.svg' },
  ];
  
  let currentIndex = 0;
  const screenshotArea = document.getElementById('screenshotArea');
  const prevBtn = document.getElementById('prevBtn');
  const nextBtn = document.getElementById('nextBtn');
  const tabs = document.querySelectorAll('.feature-tab');
  
  function updateScreenshot(index) {
    currentIndex = index;
    screenshotArea.innerHTML = `<div class="w-full h-full flex items-center justify-center text-white/30 text-sm">Screenshot: ${features[index].label}</div>`;
    
    tabs.forEach((tab, i) => {
      if (i === index) {
        tab.classList.remove('bg-white/5', 'text-white/60');
        tab.classList.add('bg-emerald-500/20', 'border-emerald-500/30', 'text-emerald-400');
      } else {
        tab.classList.remove('bg-emerald-500/20', 'border-emerald-500/30', 'text-emerald-400');
        tab.classList.add('bg-white/5', 'text-white/60');
      }
    });
  }
  
  tabs.forEach((tab, index) => {
    tab.addEventListener('click', () => updateScreenshot(index));
  });
  
  prevBtn.addEventListener('click', () => {
    const newIndex = currentIndex === 0 ? features.length - 1 : currentIndex - 1;
    updateScreenshot(newIndex);
  });
  
  nextBtn.addEventListener('click', () => {
    const newIndex = currentIndex === features.length - 1 ? 0 : currentIndex + 1;
    updateScreenshot(newIndex);
  });
  
  updateScreenshot(0);
</script>